"""Research tool stubs — swap for real APIs (search, job boards, etc.)."""


def fetch_required_skills_stub(plan: dict) -> list:
    _ = plan
    return []


def fetch_salary_stub(plan: dict) -> dict:
    _ = plan
    return {}


def fetch_market_demand_stub(plan: dict) -> dict:
    _ = plan
    return {}
